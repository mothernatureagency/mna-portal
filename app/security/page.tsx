import SecurityPanel from '@/components/security/SecurityPanel';

// Staff / standalone account-security page (full-screen, with a back link).
export default function SecurityPage() {
  return <SecurityPanel />;
}
